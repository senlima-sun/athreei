{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "dev": "bun run --watch src/index.ts",
    "start": "bun run dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@athreei/auth": "workspace:*",
    "@athreei/db": "workspace:*",
    "@athreei/email": "workspace:*",
    "@hono/zod-validator": "^0.4.1",
    "@sentry/bun": "^10.32.1",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.6.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^2.1.8"
  }
}
