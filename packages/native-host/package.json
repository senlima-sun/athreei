{
  "name": "@athreei/native-host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build ./src/index.ts --compile --outfile ./dist/athreei-host",
    "dev": "bun run --watch src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@athreei/shared": "workspace:*"
  }
}
