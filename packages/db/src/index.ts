/**
 * @athreei/db - Database package with PostgreSQL and SQLite support
 *
 * This package provides a unified Drizzle ORM interface that can connect
 * to either PostgreSQL (for platform hosting) or SQLite (for self-hosting).
 */

export * from "./schema"
export * from "./client"
